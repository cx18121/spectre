---
phase: 06-overlay-fidelity
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
files_modified:
  - overlay/src/index.css
autonomous: false
requirements:
  - OVERLAY-02
  - OVERLAY-03
  - OVERLAY-04

must_haves:
  truths:
    - "Commentary bar has --bg-mid 94% opacity background, blur(6px) backdrop filter, 1px --accent 35% opacity border, and 50% opacity SHADOW tag border"
    - "HP bar track has 1px --gold 20% opacity border AND Level 1 inset highlight (inset 0 1px 0 rgba(255,255,255,0.04))"
    - "Win dots are 8px circles with 6px gap and NO transition animation (snap to filled)"
    - "Low-HP pulse animates between opacity 1 and 0.65 (not 0.4)"
    - "HP bar fill transitions at 100ms linear (not 90ms)"
    - "Round-flash scales from 0.9 (not 0.92) with ~2010ms duration and ease-out-quart easing"
    - "KO slam scale midpoint is 0.95 (not 0.94)"
    - "All opacity-variant magic numbers in rule bodies are replaced by named var(--token) references"
    - "match-stats border, separator, and footer use --gold-border, --gold-sep, --gold-footer-rule tokens"
  artifacts:
    - path: "overlay/src/index.css"
      provides: "Full tokenized overlay stylesheet with all 18 deviations corrected"
      contains: "--gold-border"
    - path: "overlay/src/index.css"
      provides: "opacity-variant token block in :root"
      contains: "--accent-commentary-border"
    - path: "overlay/src/index.css"
      provides: "--accent-p2-bright token"
      contains: "--accent-p2-bright"
  key_links:
    - from: ":root opacity-variant tokens (--gold-border, --bg-commentary, etc.)"
      to: "rule bodies (.hp-track, .commentary-subtitle-bar, .match-stats, etc.)"
      via: "var(--token-name) references replacing inline oklch() expressions"
      pattern: "var\\(--gold-border\\)"
    - from: "@keyframes round-flash scale start"
      to: "DESIGN.md spec: scale(0.9)"
      via: "CSS keyframe correction at 0% stop"
      pattern: "scale\\(0\\.9\\)"
    - from: "@keyframes ko-slam scale midpoint"
      to: "DESIGN.md spec: scale(0.95)"
      via: "CSS keyframe correction at 50% stop"
      pattern: "scale\\(0\\.95\\)"
---

<objective>
Close all 18 DESIGN.md deviations in overlay/src/index.css via full tokenization (D-01 to D-03) and motion spec corrections (D-06, D-07).

Purpose: A systematic audit of the overlay stylesheet against DESIGN.md identified 18 concrete deviations — wrong opacity values, magic numbers used directly in rule bodies instead of tokens, missing CSS custom properties, and animation timing mismatches. This plan corrects every deviation so the running overlay matches the spec exactly.

Output:
- 7 new CSS custom property tokens added to :root (--gold-border, --gold-sep, --gold-footer-rule, --bg-commentary, --accent-commentary-border, --accent-commentary-tag-border, --accent-p2-bright)
- 8 inline oklch() expressions in rule bodies replaced with var(--token) references
- 5 animation/transition timing values corrected
- 3 win-dot dimension/behavior fixes
- 1 low-HP pulse opacity floor corrected
- Level 1 inset highlight box-shadow added to .hp-track and .match-stats
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/charliexue/School/Comps/spectre/.planning/phases/06-overlay-fidelity/06-CONTEXT.md
@/Users/charliexue/School/Comps/spectre/.planning/phases/06-overlay-fidelity/06-PATTERNS.md
@/Users/charliexue/School/Comps/spectre/DESIGN.md
@/Users/charliexue/School/Comps/spectre/overlay/src/index.css
@/Users/charliexue/School/Comps/spectre/overlay/src/components/HudLayer.tsx
@/Users/charliexue/School/Comps/spectre/overlay/src/components/RoundOverlay.tsx
@/Users/charliexue/School/Comps/spectre/overlay/src/components/CommentarySubtitle.tsx

<interfaces>
<!-- Current CSS rule line numbers (after plan-01 inserts 8 lines at top, all line numbers shift +8) -->
<!-- Executor MUST read overlay/src/index.css after plan-01 completes to see actual current line numbers. -->
<!-- The values below are the CURRENT (pre-plan-01) line numbers for reference; add ~8 for post-plan-01 state. -->

Current :root block (lines 1-20 pre-plan-01 / ~9-28 post-plan-01):
  No opacity-variant tokens exist yet — only 10 palette tokens.
  Add new block immediately after the last existing token (--text-dim) and before the font-family declaration.

Current deviations with exact current values (read the file first to confirm line numbers):

  CSS-01: .hp-track border (line ~242+8)
    Current:  border: 1px solid oklch(from var(--gold) l c h / 0.18);
    Required: border: 1px solid var(--gold-border);
    Token:    --gold-border: oklch(from var(--gold) l c h / 0.20)

  CSS-02: .commentary-subtitle-bar backdrop-filter (line ~756+8)
    Current:  backdrop-filter: blur(8px);
    Required: backdrop-filter: blur(6px);
    (No token needed — correct the literal value)

  CSS-03: .commentary-subtitle-bar background (line ~752+8)
    Current:  background: oklch(from var(--bg-mid) l c h / 0.94);
    Required: background: var(--bg-commentary);
    Token:    --bg-commentary: oklch(from var(--bg-mid) l c h / 0.94)

  CSS-04: .commentary-subtitle-bar border (line ~753+8)
    Current:  border: 1px solid oklch(from var(--accent) l c h / 0.35);
    Required: border: 1px solid var(--accent-commentary-border);
    Token:    --accent-commentary-border: oklch(from var(--accent) l c h / 0.35)

  CSS-05: .commentary-subtitle-tag border (line ~766+8)
    Current:  border: 1px solid oklch(from var(--accent) l c h / 0.5);
    Required: border: 1px solid var(--accent-commentary-tag-border);
    Token:    --accent-commentary-tag-border: oklch(from var(--accent) l c h / 0.50)

  CSS-06: .match-stats border (line ~357+8)
    Current:  border: 1px solid oklch(from var(--gold) l c h / 0.18);
    Required: border: 1px solid var(--gold-border);
    (same --gold-border token as CSS-01, value already correct at 0.20)

  CSS-07: .match-stats-sep background (line ~374+8)
    Current:  background: oklch(from var(--gold) l c h / 0.14);
    Required: background: var(--gold-sep);
    Token:    --gold-sep: oklch(from var(--gold) l c h / 0.14)

  CSS-08: .match-stats-footer border-top (line ~379+8)
    Current:  border-top: 1px solid oklch(from var(--gold) l c h / 0.12);
    Required: border-top: 1px solid var(--gold-footer-rule);
    Token:    --gold-footer-rule: oklch(from var(--gold) l c h / 0.12)

  CSS-09: .hp-fill transition (line ~248+8)
    Current:  transition: width 90ms linear;
    Required: transition: width 100ms linear;

  CSS-10: .win-dot width + height (line ~212+8)
    Current:  width: 7px; height: 7px;
    Required: width: 8px; height: 8px;

  CSS-11: .win-dots gap (line ~208+8)
    Current:  gap: 5px;
    Required: gap: 6px;

  CSS-12: .win-dot transition (line ~215-216+8)
    Current:  transition: background 180ms ease, border-color 180ms ease;
    Required: (remove the transition property entirely — snap to filled per DESIGN.md)

  CSS-13: @keyframes hp-pulse opacity floor (line ~260+8)
    Current:  50% { opacity: 0.4; }
    Required: 50% { opacity: 0.65; }

  CSS-14: @keyframes round-flash scale start (line ~790+8)
    Current:  0% { opacity: 0; transform: scale(0.92); }
    Required: 0% { opacity: 0; transform: scale(0.9); }

  CSS-15: .round-flash animation (line ~298+8 on .round-flash rule)
    Current:  animation: round-flash 2s ease both;
    Required: animation: round-flash 2.01s cubic-bezier(0.25, 1, 0.5, 1) both;
    (2.01s = 160ms appear + 1500ms hold + 350ms fade; ease-out-quart = cubic-bezier(0.25,1,0.5,1))

  CSS-16: @keyframes ko-slam scale midpoint (line ~797+8)
    Current:  50% { opacity: 1; transform: scale(0.94) translateY(0); }
    Required: 50% { opacity: 1; transform: scale(0.95) translateY(0); }

  CSS-17: (handled in plan-01 — @font-face)

  CSS-18: --accent-p2-bright missing from :root
    Current:  absent from :root (value oklch(62% 0.2 250) used inline at .match-stat-value.p2)
    Required: add --accent-p2-bright: oklch(62% 0.2 250) to :root tokens
    Also replace: .match-stat-value.p2 { color: oklch(62% 0.2 250); }
           with:  .match-stat-value.p2 { color: var(--accent-p2-bright); }

  MISSING (not in CSS-XX list but required by DESIGN.md Level 1 elevation spec):
    .hp-track currently has: border only — MISSING the Level 1 inset highlight
    Required: box-shadow: inset 0 1px 0 rgba(255,255,255,0.04);  (add to .hp-track)
    .match-stats currently has: border only — MISSING the Level 1 inset highlight
    Required: box-shadow: inset 0 1px 0 rgba(255,255,255,0.04);  (add to .match-stats)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add opacity-variant tokens to :root and tokenize rule bodies</name>
  <files>overlay/src/index.css</files>
  <read_first>
    - overlay/src/index.css — read full file after plan-01 completion to get current line numbers
    - DESIGN.md lines 60-68 (Elevation spec) — Level 1 = 1px border at --gold 20% opacity + inset 0 1px 0 rgba(255,255,255,0.04)
    - DESIGN.md lines 100-109 (Commentary Bar spec) — bg-mid 94% opacity, blur(6px), --accent 35% border, --accent 50% tag border
  </read_first>
  <action>
    Perform the following edits to overlay/src/index.css in a single pass. Read the file first to confirm current line numbers.

    STEP 1 — Add token block to :root immediately after --text-dim line and before the font-family declaration.
    Insert these 8 new tokens (use the same column-alignment style as the existing palette):

    ```css
      /* opacity-variant semantic tokens (D-01) */
      --gold-border:                oklch(from var(--gold) l c h / 0.20);
      --gold-sep:                   oklch(from var(--gold) l c h / 0.14);
      --gold-footer-rule:           oklch(from var(--gold) l c h / 0.12);
      --bg-commentary:              oklch(from var(--bg-mid) l c h / 0.94);
      --accent-commentary-border:   oklch(from var(--accent) l c h / 0.35);
      --accent-commentary-tag-border: oklch(from var(--accent) l c h / 0.50);
      --accent-p2-bright:           oklch(62% 0.2 250);
    ```

    STEP 2 — Replace inline expressions in rule bodies with var() references (per D-03):

      a. .hp-track border:
         Change:  border: 1px solid oklch(from var(--gold) l c h / 0.18);
         To:      border: 1px solid var(--gold-border);

      b. .hp-track — add Level 1 inset highlight (new line after the border line):
         Add:     box-shadow: inset 0 1px 0 rgba(255,255,255,0.04);

      c. .commentary-subtitle-bar background:
         Change:  background: oklch(from var(--bg-mid) l c h / 0.94);
         To:      background: var(--bg-commentary);

      d. .commentary-subtitle-bar border:
         Change:  border: 1px solid oklch(from var(--accent) l c h / 0.35);
         To:      border: 1px solid var(--accent-commentary-border);

      e. .commentary-subtitle-bar backdrop-filter:
         Change:  backdrop-filter: blur(8px);
         To:      backdrop-filter: blur(6px);

      f. .commentary-subtitle-tag border:
         Change:  border: 1px solid oklch(from var(--accent) l c h / 0.5);
         To:      border: 1px solid var(--accent-commentary-tag-border);

      g. .match-stats border:
         Change:  border: 1px solid oklch(from var(--gold) l c h / 0.18);
         To:      border: 1px solid var(--gold-border);

      h. .match-stats — add Level 1 inset highlight (new line after the border line):
         Add:     box-shadow: inset 0 1px 0 rgba(255,255,255,0.04);

      i. .match-stats-sep background:
         Change:  background: oklch(from var(--gold) l c h / 0.14);
         To:      background: var(--gold-sep);

      j. .match-stats-footer border-top:
         Change:  border-top: 1px solid oklch(from var(--gold) l c h / 0.12);
         To:      border-top: 1px solid var(--gold-footer-rule);

      k. .match-stat-value.p2 color:
         Change:  color: oklch(62% 0.2 250);
         To:      color: var(--accent-p2-bright);
  </action>
  <verify>
    <automated>
      grep -v "^[[:space:]]*/\*" /Users/charliexue/School/Comps/spectre/overlay/src/index.css | grep -c "oklch(from var(--gold) l c h / 0\.18)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\-\-gold-border:" overlay/src/index.css` outputs 1 (token defined once in :root)
    - `grep -c "\-\-bg-commentary:" overlay/src/index.css` outputs 1
    - `grep -c "\-\-accent-commentary-border:" overlay/src/index.css` outputs 1
    - `grep -c "\-\-accent-commentary-tag-border:" overlay/src/index.css` outputs 1
    - `grep -c "\-\-gold-sep:" overlay/src/index.css` outputs 1
    - `grep -c "\-\-gold-footer-rule:" overlay/src/index.css` outputs 1
    - `grep -c "\-\-accent-p2-bright:" overlay/src/index.css` outputs 1
    - `grep "oklch(from var(--gold) l c h / 0.18)" overlay/src/index.css` produces NO output (the 0.18 expressions are gone)
    - `grep "oklch(from var(--bg-mid) l c h / 0.94)" overlay/src/index.css` produces NO output outside :root (only the token definition in :root remains)
    - `grep "oklch(from var(--accent) l c h / 0.35)" overlay/src/index.css` produces NO output outside :root
    - `grep "oklch(from var(--accent) l c h / 0.5)" overlay/src/index.css` produces NO output outside :root
    - `grep "oklch(62% 0.2 250)" overlay/src/index.css` matches only the :root token definition, not .match-stat-value.p2
    - `grep "backdrop-filter: blur(8px)" overlay/src/index.css` produces NO output
    - `grep "backdrop-filter: blur(6px)" overlay/src/index.css` matches .commentary-subtitle-bar
    - `grep "box-shadow: inset 0 1px 0 rgba(255,255,255,0.04)" overlay/src/index.css` outputs 2 lines (one for .hp-track, one for .match-stats)
    - `grep "var(--gold-border)" overlay/src/index.css` outputs 2 lines (.hp-track and .match-stats)
  </acceptance_criteria>
  <done>All opacity-variant tokens defined in :root. All inline oklch() expressions in rule bodies replaced with var(--token) references. Level 1 inset highlight added to .hp-track and .match-stats. backdrop-filter corrected from blur(8px) to blur(6px). --accent-p2-bright added to :root and wired to .match-stat-value.p2.</done>
</task>

<task type="auto">
  <name>Task 2: Fix win dots, HP timing, low-HP pulse, and animation spec corrections</name>
  <files>overlay/src/index.css</files>
  <read_first>
    - overlay/src/index.css — read full file (after Task 1 edits) to confirm current line numbers for win-dot rules, hp-fill, hp-pulse keyframes, round-flash keyframes, round-flash element rule, ko-slam keyframes
    - DESIGN.md lines 95-99 (Win Dots spec) — 8px circle, 6px gap, no animation
    - DESIGN.md lines 87-93 (HP Bar spec) — 100ms linear, low-HP pulse 1→0.65→1
    - DESIGN.md lines 76-77 (Motion: round flash, KO slam)
  </read_first>
  <action>
    Perform the following targeted edits to overlay/src/index.css. Read the file first to confirm current line numbers.

    STEP 1 — Win dots (CSS-10, CSS-11, CSS-12):

      a. .win-dots gap:
         Change:  gap: 5px;
         To:      gap: 6px;

      b. .win-dot width and height:
         Change:  width: 7px;
                  height: 7px;
         To:      width: 8px;
                  height: 8px;

      c. .win-dot transition — REMOVE the entire transition property:
         Delete the line:  transition: background 180ms ease, border-color 180ms ease;
         (Win dots must snap to filled state with no animation — DESIGN.md spec)

    STEP 2 — HP bar fill transition (CSS-09):
      Change:  transition: width 90ms linear;
      To:      transition: width 100ms linear;

    STEP 3 — Low-HP pulse floor (CSS-13):
      In @keyframes hp-pulse:
      Change:  50% { opacity: 0.4; }
      To:      50% { opacity: 0.65; }

    STEP 4 — Round-flash animation (CSS-14, CSS-15):

      a. In @keyframes round-flash, 0% stop:
         Change:  0% { opacity: 0; transform: scale(0.92); }
         To:      0% { opacity: 0; transform: scale(0.9); }

      b. On the .round-flash element rule, the animation property:
         Change:  animation: round-flash 2s ease both;
         To:      animation: round-flash 2.01s cubic-bezier(0.25, 1, 0.5, 1) both;
         (2.01s = 160ms appear + 1500ms hold + 350ms fade per DESIGN.md Motion spec;
          cubic-bezier(0.25, 1, 0.5, 1) = ease-out-quart)

    STEP 5 — KO slam scale midpoint (CSS-16):
      In @keyframes ko-slam, 50% stop:
      Change:  50% { opacity: 1; transform: scale(0.94) translateY(0); }
      To:      50% { opacity: 1; transform: scale(0.95) translateY(0); }

      The .ko-text animation property currently reads `animation: ko-slam 0.5s cubic-bezier(0.34, 1.15, 0.64, 1) both`.
      DESIGN.md spec says 480ms. Change 0.5s to 480ms:
      Change:  animation: ko-slam 0.5s cubic-bezier(0.34, 1.15, 0.64, 1) both;
      To:      animation: ko-slam 480ms cubic-bezier(0.34, 1.15, 0.64, 1) both;
  </action>
  <verify>
    <automated>
      grep -E "gap: 6px|width: 8px|height: 8px|opacity: 0\.65|scale\(0\.9\)|scale\(0\.95\)|480ms|2\.01s|100ms linear" /Users/charliexue/School/Comps/spectre/overlay/src/index.css
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "gap: 6px" overlay/src/index.css` matches .win-dots rule
    - `grep "width: 8px" overlay/src/index.css` matches .win-dot rule
    - `grep "height: 8px" overlay/src/index.css` matches .win-dot rule
    - `grep "transition: background 180ms ease" overlay/src/index.css` produces NO output (transition removed)
    - `grep "transition: width 90ms" overlay/src/index.css` produces NO output
    - `grep "transition: width 100ms linear" overlay/src/index.css` matches .hp-fill rule
    - `grep "opacity: 0\.4" overlay/src/index.css` produces NO output (old pulse floor gone)
    - `grep "opacity: 0\.65" overlay/src/index.css` matches the 50% stop of @keyframes hp-pulse
    - `grep "scale(0\.92)" overlay/src/index.css` produces NO output (old round-flash scale gone)
    - `grep "scale(0\.9)" overlay/src/index.css` matches the 0% stop of @keyframes round-flash (note: also matches 0.92-less scale(0.9) variants — confirm it's exactly scale(0.9) not scale(0.9X))
    - `grep "2\.01s" overlay/src/index.css` matches the .round-flash animation property
    - `grep "cubic-bezier(0\.25, 1, 0\.5, 1)" overlay/src/index.css` matches the .round-flash animation property
    - `grep "scale(0\.94)" overlay/src/index.css` produces NO output (old ko-slam scale gone)
    - `grep "scale(0\.95)" overlay/src/index.css` matches the 50% stop of @keyframes ko-slam
    - `grep "480ms" overlay/src/index.css` matches .ko-text animation property
  </acceptance_criteria>
  <done>Win dots are 8px/6px gap with no transition. HP fill transition is 100ms. Low-HP pulse floor is 0.65. Round-flash scales from 0.9 with 2.01s ease-out-quart. KO slam hits scale(0.95) midpoint at 480ms.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 18 CSS deviations corrected in overlay/src/index.css:
    - 7 new opacity-variant tokens in :root
    - 8 inline oklch expressions replaced with var() references
    - Commentary bar: blur(6px), correct border/background tokens
    - HP track: --gold-border (0.20 opacity) + Level 1 inset highlight
    - Match stats: --gold-border + Level 1 inset highlight + --gold-sep + --gold-footer-rule
    - Win dots: 8px, 6px gap, no transition
    - HP fill: 100ms linear
    - Low-HP pulse: floors at 0.65
    - Round-flash: scale(0.9) start, 2.01s ease-out-quart
    - KO slam: scale(0.95) midpoint, 480ms
    - --accent-p2-bright added to :root, wired to .match-stat-value.p2
  </what-built>
  <how-to-verify>
    1. Start the dev server (from the overlay directory):
       cd overlay && npm run dev

    2. Open the overlay in a browser at the URL shown in the terminal output (typically http://localhost:5173).

    3. Check these specific visual elements:

       Win dots (if visible in waiting state):
       - Dots should be visually 8px circles with 6px spacing
       - When a dot fills, it should snap instantly — no fade-in animation

       HP bars (during a live match or inspect via DevTools):
       - HP track has a visible gold border (slightly warmer/brighter than 0.18 opacity)
       - No obvious visual discontinuity (the 0.18 → 0.20 change is subtle)

       Commentary bar (if commentary is active):
       - Background appears as a semi-transparent dark panel (--bg-mid 94% opacity)
       - Backdrop blur is present (elements behind bar are blurred)
       - SHADOW tag has a visible accent border

       Round announcements (trigger by watching a countdown):
       - "3", "2", "1", "FIGHT!" should render in a display typeface (Achafont from plan-01)
       - Text should appear to "pop" from slightly smaller to full size (scale 0.9→1)

    4. Open browser DevTools → Console tab:
       - No 404 errors for /fonts/Achafont.ttf (plan-01 verification)
       - No CSS errors

    5. Open DevTools → Elements → inspect .hp-track:
       - border should show as `1px solid oklch(78% 0.11 85 / 0.20)` (resolved from --gold-border)
       - box-shadow should include `inset 0 1px 0 rgba(255, 255, 255, 0.04)`

    6. Inspect .commentary-subtitle-bar:
       - backdrop-filter: blur(6px) — NOT blur(8px)
  </how-to-verify>
  <resume-signal>Type "approved" if the overlay matches the visual spec, or describe any remaining issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS custom properties → computed styles | Token values defined in :root cascade to all descendants; no cross-origin concerns |
| Static asset serving → browser | overlay/src/index.css is a build artifact served by Vite; no auth, public read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | CSS custom property override | accept | CSS variable override via DevTools or injected stylesheets is a browser-level capability; no mitigation needed for a local overlay app |
| T-06-02-02 | Denial of Service | backdrop-filter performance | accept | blur(6px) is a minor GPU filter on a single element; performance acceptable on gaming hardware running this overlay |
| T-06-02-03 | Information Disclosure | OKLCH computed values visible in DevTools | accept | Color tokens encode no sensitive data; design system values are non-sensitive |
</threat_model>

<verification>
After Task 1 and Task 2 complete (before the human checkpoint):

Run these grep checks from the overlay directory root:

1. Token definitions:
   grep -E "\-\-(gold-border|gold-sep|gold-footer-rule|bg-commentary|accent-commentary-border|accent-commentary-tag-border|accent-p2-bright):" overlay/src/index.css
   Expected: 7 lines, one per token

2. No remaining raw 0.18 expressions:
   grep "/ 0\.18)" overlay/src/index.css
   Expected: no output

3. No remaining blur(8px) on commentary:
   grep "blur(8px)" overlay/src/index.css
   Expected: no output (settings-panel uses blur(8px) — that is NOT commentary-subtitle-bar; verify it is untouched)

4. Level 1 inset highlights:
   grep -c "inset 0 1px 0 rgba(255,255,255,0.04)" overlay/src/index.css
   Expected: 2

5. Win dot size:
   grep -A5 "\.win-dot {" overlay/src/index.css | grep -E "width|height|transition"
   Expected: width: 8px, height: 8px, no transition line

6. Animation values:
   grep "round-flash 2" overlay/src/index.css
   Expected: animation: round-flash 2.01s cubic-bezier(0.25, 1, 0.5, 1) both

   grep "ko-slam" overlay/src/index.css | grep -v "@keyframes"
   Expected: animation: ko-slam 480ms cubic-bezier(0.34, 1.15, 0.64, 1) both
</verification>

<success_criteria>
- OVERLAY-02: commentary bar has --bg-mid 94% opacity background (var(--bg-commentary)), backdrop-filter blur(6px), 1px var(--accent-commentary-border) border, SHADOW tag with var(--accent-commentary-tag-border) border
- OVERLAY-03: .hp-track uses var(--gold-border) border (0.20 opacity) AND box-shadow inset 0 1px 0 rgba(255,255,255,0.04) (Level 1 elevation); same for .match-stats
- OVERLAY-04: win dots are 8px/6px gap with no transition; HP drain is 100ms linear; low-HP pulse floors at 0.65; round-flash scales from 0.9 at 2.01s ease-out-quart; ko-slam midpoint is scale(0.95) at 480ms; --accent-p2-bright token defined and wired
- D-01/D-02/D-03 tokenization policy: no raw oklch() opacity expressions remain in rule bodies outside :root; every opacity variant has a named custom property
- Human checkpoint passed: overlay visually matches DESIGN.md spec
</success_criteria>

<output>
After completion, create .planning/phases/06-overlay-fidelity/06-02-SUMMARY.md
</output>
