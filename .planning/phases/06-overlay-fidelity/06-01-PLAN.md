---
phase: 06-overlay-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - overlay/public/fonts/Achafont.ttf
  - overlay/public/fonts/Achafout.ttf
  - overlay/src/index.css
autonomous: true
requirements:
  - OVERLAY-01

must_haves:
  truths:
    - "Countdown numbers (3, 2, 1), FIGHT!, KO text, and match-end title render in Achafont — visually distinct from Inter body text"
    - "Font loads without FOIT — text is visible immediately via Inter fallback during load, then swaps"
    - "No @font-face missing-file errors in browser console when overlay is served"
  artifacts:
    - path: "overlay/public/fonts/Achafont.ttf"
      provides: "Achafont TrueType font binary"
      min_lines: 1
    - path: "overlay/public/fonts/Achafout.ttf"
      provides: "Achafout variant TrueType font binary"
      min_lines: 1
    - path: "overlay/src/index.css"
      provides: "@font-face declaration for 'Achafont'"
      contains: "@font-face"
  key_links:
    - from: "overlay/src/index.css @font-face"
      to: "overlay/public/fonts/Achafont.ttf"
      via: "url('/fonts/Achafont.ttf') format('truetype') — Vite serves public/ at root"
      pattern: "url\\('/fonts/Achafont\\.ttf'\\)"
    - from: ".round-flash, .ko-text, .match-end-title, .waiting-title, .waiting-vs"
      to: "@font-face { font-family: 'Achafont' }"
      via: "font-family: 'Achafont' already present in 5 rules — @font-face name must match exactly"
      pattern: "font-family: 'Achafont'"
---

<objective>
Restore the Achafont display typeface so every DESIGN.md drama element renders as intended.

Purpose: The overlay CSS already references `font-family: 'Achafont'` in 5 rules (.round-flash, .ko-text, .match-end-title, .waiting-title, .waiting-vs) but the font file and @font-face declaration are both absent — all those elements silently fall back to Inter. This plan recovers the TTF from git history and adds the single @font-face rule that activates the font at runtime.

Output:
- overlay/public/fonts/Achafont.ttf — recovered binary
- overlay/public/fonts/Achafout.ttf — recovered variant binary
- @font-face declaration prepended to overlay/src/index.css (before the :root block)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/charliexue/School/Comps/spectre/.planning/phases/06-overlay-fidelity/06-CONTEXT.md
@/Users/charliexue/School/Comps/spectre/.planning/phases/06-overlay-fidelity/06-PATTERNS.md
@/Users/charliexue/School/Comps/spectre/DESIGN.md
@/Users/charliexue/School/Comps/spectre/overlay/src/index.css

<interfaces>
<!-- Font name already used in 5 CSS rules — @font-face family name MUST match exactly. -->

From overlay/src/index.css — existing Achafont references:
  Line 291:  font-family: 'Achafont', Inter, sans-serif;   /* .round-flash */
  Line 327:  font-family: 'Achafont', Inter, sans-serif;   /* .ko-text */
  Line 338:  font-family: 'Achafont', Inter, sans-serif;   /* .match-end-title */
  Line 490:  font-family: 'Achafont', Inter, sans-serif;   /* .waiting-title */
  Line 572:  font-family: 'Achafont', Inter, sans-serif;   /* .waiting-vs */

Vite static asset URL convention (overlay project uses Vite):
  Files in overlay/public/ are served at the URL root.
  overlay/public/fonts/Achafont.ttf  →  URL: /fonts/Achafont.ttf
  Use url('/fonts/Achafont.ttf') in @font-face src — NOT a relative path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recover font files from git history</name>
  <files>overlay/public/fonts/Achafont.ttf, overlay/public/fonts/Achafout.ttf</files>
  <read_first>
    - Run: git show 4de2977 --name-only | grep fonts
      (confirms which font filenames exist in commit 4de2977 before extracting)
    - Run: ls overlay/public/fonts/ 2>/dev/null || echo "directory does not exist"
      (confirms the fonts directory must be created)
  </read_first>
  <action>
    1. Create the fonts directory:
       mkdir -p overlay/public/fonts

    2. Extract both font files from git commit 4de2977 using binary-safe redirection:
       git show 4de2977:overlay/public/fonts/Achafont.ttf > overlay/public/fonts/Achafont.ttf
       git show 4de2977:overlay/public/fonts/Achafout.ttf > overlay/public/fonts/Achafout.ttf

    3. Sanity-check that both files are non-empty:
       ls -lh overlay/public/fonts/

    If git show reports "path not in tree", first inspect the commit with:
       git show 4de2977 --name-only | grep -i font
    and adjust the path accordingly. The CONTEXT.md confirms both files exist in that commit.
  </action>
  <verify>
    <automated>
      ls -la /Users/charliexue/School/Comps/spectre/overlay/public/fonts/Achafont.ttf \
             /Users/charliexue/School/Comps/spectre/overlay/public/fonts/Achafout.ttf \
      && echo "PASS: both font files present"
    </automated>
  </verify>
  <acceptance_criteria>
    - overlay/public/fonts/Achafont.ttf exists and has file size > 0 bytes
    - overlay/public/fonts/Achafout.ttf exists and has file size > 0 bytes
    - `file overlay/public/fonts/Achafont.ttf` reports TrueType or OpenType font data (not an error message or empty file)
    - Neither file is a git error message written to disk (check: `head -c 4 overlay/public/fonts/Achafont.ttf | xxd` should show binary, not "fata" from "fatal:")
  </acceptance_criteria>
  <done>Both TTF files extracted from git commit 4de2977 and placed in overlay/public/fonts/. Directory created. Files are non-empty binary font data.</done>
</task>

<task type="auto">
  <name>Task 2: Add @font-face declaration to overlay CSS</name>
  <files>overlay/src/index.css</files>
  <read_first>
    - overlay/src/index.css — read the full file to see current :root block start (line 1) and verify no @font-face already exists
    - DESIGN.md lines 38-39 — typography rules for Achafont usage scope
  </read_first>
  <action>
    Edit overlay/src/index.css to prepend the @font-face declaration BEFORE the :root block (i.e., insert at line 1, pushing :root down).

    The exact text to insert at the top of the file (before :root {):

    ```css
    @font-face {
      font-family: 'Achafont';
      src: url('/fonts/Achafont.ttf') format('truetype');
      font-weight: 100 950;
      font-style: normal;
      font-display: swap;
    }

    ```

    Rules:
    - font-family value is 'Achafont' — single quotes, capital A, matches the 5 existing call sites exactly.
    - src path is '/fonts/Achafont.ttf' — absolute URL with leading slash (Vite serves overlay/public/ at the root).
    - font-weight: 100 950 — variable weight range covers the 950 weight used by .round-flash and .ko-text.
    - font-display: swap — prevents FOIT (invisible text during load); Inter is already rendering so swap is safe.
    - Do NOT add a second @font-face for Achafout.ttf — the overlay CSS references only 'Achafont'. Achafout.ttf is recovered as a companion file but not declared in CSS.
    - Do NOT modify any existing CSS rules — only insert the @font-face block at the top.
  </action>
  <verify>
    <automated>
      grep -n "@font-face" /Users/charliexue/School/Comps/spectre/overlay/src/index.css
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@font-face" overlay/src/index.css` outputs 1 (exactly one @font-face rule)
    - `grep "font-family: 'Achafont'" overlay/src/index.css | head -1` shows the @font-face family declaration (the @font-face rule itself, not the call sites)
    - `grep "url('/fonts/Achafont.ttf')" overlay/src/index.css` matches
    - `grep "font-display: swap" overlay/src/index.css` matches
    - `grep "font-weight: 100 950" overlay/src/index.css` matches
    - `grep -n "@font-face" overlay/src/index.css` reports line 1 (declaration is first in file)
    - The 5 existing call sites (lines ~291, ~327, ~338, ~490, ~572 post-insertion) are unchanged — still `font-family: 'Achafont', Inter, sans-serif;`
  </acceptance_criteria>
  <done>@font-face declaration for 'Achafont' is the first rule in overlay/src/index.css. Font will load from /fonts/Achafont.ttf with font-display: swap. All 5 existing Achafont references are now backed by the actual font file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history → filesystem | Font files recovered from a specific commit hash — no external network required |
| public/fonts/ → browser | Static font assets served by Vite dev server / bundler; no auth, public read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | git show output redirect | accept | Recovery is from local git history (trusted source); commit hash 4de2977 is pinned and deterministic |
| T-06-01-02 | Information Disclosure | /fonts/ public directory | accept | Font files are non-sensitive static assets; public serving is intentional and consistent with /sfx/ pattern |
| T-06-01-03 | Denial of Service | font-display: swap causing layout shift | mitigate | font-display: swap is explicitly chosen to avoid FOIT; Inter is already rendered so CLS is minimal |
</threat_model>

<verification>
After both tasks complete:

1. Confirm font files exist and are binary:
   ls -lh overlay/public/fonts/
   file overlay/public/fonts/Achafont.ttf   # should say TrueType

2. Confirm @font-face is first rule in CSS:
   head -8 overlay/src/index.css   # should show the @font-face block

3. Confirm no existing font-family call sites were disturbed:
   grep -c "font-family: 'Achafont'" overlay/src/index.css   # should output 6 (1 @font-face + 5 call sites)

4. Run Vite dev server and open overlay in browser — elements with class .round-flash, .ko-text should render with the display typeface, visually distinct from Inter labels.
</verification>

<success_criteria>
- overlay/public/fonts/Achafont.ttf is a non-empty TrueType file recovered from commit 4de2977
- overlay/public/fonts/Achafout.ttf is a non-empty TrueType file recovered from commit 4de2977
- overlay/src/index.css line 1 begins with @font-face
- @font-face declares font-family: 'Achafont', src: url('/fonts/Achafont.ttf') format('truetype'), font-weight: 100 950, font-display: swap
- All 5 pre-existing font-family: 'Achafont' call sites are unchanged
- OVERLAY-01 requirement satisfied: Achafont loaded via @font-face from overlay/public/fonts/Achafont.ttf
</success_criteria>

<output>
After completion, create .planning/phases/06-overlay-fidelity/06-01-SUMMARY.md
</output>
