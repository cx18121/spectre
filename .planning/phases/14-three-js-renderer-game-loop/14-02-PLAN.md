---
phase: 14-three-js-renderer-game-loop
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - fps/src/lib/springPhysics.ts
  - fps/src/lib/guardDetection.ts
  - fps/src/hooks/useGameRenderer.ts
autonomous: true
requirements:
  - FPR-02
  - FPR-03
  - GML-04

must_haves:
  truths:
    - "Player arms visually extend/stretch when a punch is thrown; extension magnitude scales with wrist velocity"
    - "Arms overshoot the wrist target and spring back (under-damped oscillation visible on fast punches)"
    - "Opponent arm positions lerp smoothly between 30Hz server ticks; no jitter or snapping"
    - "Player raising both wrists above shoulder height activates guard state (with 3-frame entry hysteresis)"
    - "Guard state is available as a ref that future plans (14-04) can read for damage reduction"
  artifacts:
    - path: "fps/src/lib/springPhysics.ts"
      provides: "SpringState interface, stepSpring() semi-implicit Euler integrator"
      exports: ["SpringState", "stepSpring"]
    - path: "fps/src/lib/guardDetection.ts"
      provides: "isGuardPose() pure function, GuardState interface, updateGuard() hysteresis"
      exports: ["isGuardPose", "GuardState", "updateGuard"]
  key_links:
    - from: "fps/src/hooks/useGameRenderer.ts"
      to: "fps/src/lib/springPhysics.ts"
      via: "stepSpring() called per arm in tick(), driving forearm mesh scale.z (extension)"
      pattern: "stepSpring"
    - from: "fps/src/hooks/useGameRenderer.ts"
      to: "fps/src/lib/guardDetection.ts"
      via: "updateGuard() called in tick() with isGuardPose() result; guardStateRef exposed for Plan 14-04"
      pattern: "updateGuard"
---

<objective>
Add spring physics to player arm extension (FPR-02), frame-rate-independent lerp for opponent arms (FPR-03 completion), and guard detection with hysteresis (GML-04). All changes extend useGameRenderer.ts — no new React components.

Purpose: Make punches feel weighty (spring overshoot) and opponent rendering smooth (lerp at 60fps from 30Hz data). Guard pose unlocks damage reduction wiring in Plan 14-04.

Output: Arms that stretch on fast punches, lerped opponent arms, and guard detection active in the game loop.

Note on naming: PATTERNS.md refers to these as `useSpring.ts` and `useCameraShake.ts` (React hooks). This plan implements them as plain library modules (`springPhysics.ts`, `guardDetection.ts`) inside fps/src/lib/ — no React hook wrapper — because spring/guard state lives in the game loop's `useRef` collection, not React state. The `files_modified` frontmatter reflects the actual filenames created. PATTERNS.md's hook names are the conceptual role; the implementation is a plain module pattern as noted in PATTERNS.md itself ("may be a plain module export... rather than a React hook").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-three-js-renderer-game-loop/14-CONTEXT.md
@.planning/phases/14-three-js-renderer-game-loop/14-RESEARCH.md
@.planning/phases/14-three-js-renderer-game-loop/14-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 14-01 output and existing utilities -->

From fps/src/lib/springPhysics.ts (to be created — contract for useGameRenderer):
```typescript
export interface SpringState { pos: number; vel: number; }
export function stepSpring(
  state: SpringState,
  target: number,
  dt: number,
  stiffness?: number,   // default 300
  damping?: number,     // default 18
): void;
// Semi-implicit Euler: vel += force * dt; pos += vel * dt
// stiffness=300, damping=18 → under-damped (ζ≈0.52), ~1.5 oscillation cycles
```

From fps/src/lib/guardDetection.ts (to be created — contract for useGameRenderer):
```typescript
export function isGuardPose(keypoints: PoseKeypoint[] | null, threshold?: number): boolean;
// threshold default 0.05: wrist.y must be < shoulder.y by this amount in world coords
// [ASSUMED A2]: Y sign — verified in Plan 14-01 Task 5; confirm result before implementing

export interface GuardState { active: boolean; consecutiveFrames: number; }
export function updateGuard(state: GuardState, raw: boolean): void;
// ENTER_FRAMES=3: guard activates after 3 consecutive raw=true frames
// EXIT_FRAMES=5: guard deactivates after 5 consecutive raw=false frames
```

From fps/src/lib/velocity.ts (already exists):
```typescript
export const LANDMARK = { LEFT_SHOULDER: 11, RIGHT_SHOULDER: 12,
  LEFT_ELBOW: 13, RIGHT_ELBOW: 14, LEFT_WRIST: 15, RIGHT_WRIST: 16 } as const;
export function computeWristPeakSpeed(frames: TimedFrame[], wrist: 'left' | 'right'): number;
```

From fps/src/hooks/useGameRenderer.ts (created in Plan 14-01):
Spring state and guard state will be added as new useRef entries:
```typescript
// New refs to add inside useGameRenderer:
springStateRef: { left: SpringState, right: SpringState }
guardStateRef: GuardState            // exported via returned object for Plan 14-04
opponentTargetRef: { ... }           // current opponent target positions (lerp targets)
```

Opponent lerp: frame-rate-independent exponential lerp
```typescript
// lambda=12 → reaches 99% of target in ~460ms; smooth for 30Hz server ticks
const alpha = 1 - Math.exp(-12 * dt);
opponentShoulderL.lerp(targetShoulderL, alpha);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: springPhysics.ts and guardDetection.ts pure utility modules</name>
  <files>fps/src/lib/springPhysics.ts, fps/src/lib/guardDetection.ts</files>
  <read_first>
    - fps/src/lib/velocity.ts — import style, LANDMARK constants, pure function module structure
    - fps/src/lib/coordinateMap.ts (created in Plan 14-01) — confirm module pattern to replicate
    - .planning/phases/14-three-js-renderer-game-loop/14-RESEARCH.md sections Q3 (spring integrator) and Q10 (guard detection)
    - .planning/phases/14-three-js-renderer-game-loop/14-PATTERNS.md — useSpring and guardDetection pattern sections
    - .planning/phases/14-three-js-renderer-game-loop/14-01-SUMMARY.md — confirm Y-axis sign result from Task 5 spike before implementing isGuardPose comparison direction
  </read_first>
  <action>
    Create fps/src/lib/springPhysics.ts:
    - Export `interface SpringState { pos: number; vel: number; }`
    - Export `function stepSpring(state: SpringState, target: number, dt: number, stiffness = 300, damping = 18): void`
    - Implementation: `const force = stiffness * (target - state.pos) - damping * state.vel; state.vel += force * dt; state.pos += state.vel * dt;`
    - Add JSDoc: "Semi-implicit Euler — stable for stiffness up to 2/dt² (~7200 at 60fps). stiffness=300, damping=18 gives under-damped (ζ≈0.52) overshoot. Tune during implementation."
    - No imports needed (plain numbers only).

    Create fps/src/lib/guardDetection.ts:
    - Import: `import type { PoseKeypoint } from '@shared/protocol'` and `import { LANDMARK } from './velocity'`
    - Export `interface GuardState { active: boolean; consecutiveFrames: number; }`
    - Export `function isGuardPose(keypoints: PoseKeypoint[] | null, threshold = 0.05): boolean` — guards: return false if keypoints null or length <= LANDMARK.RIGHT_WRIST. Compare `keypoints[LANDMARK.LEFT_SHOULDER].y - keypoints[LANDMARK.LEFT_WRIST].y > threshold` (both wrists must be above both shoulders). Add JSDoc: "Y sign must match result from Plan 14-01 Task 5 spike. If Y-axis is positive-up (negation applied in keypointToWorld), wrist.y > shoulder.y when raised — negate the comparison. Read 14-01-SUMMARY.md for confirmed sign before shipping."
    - Export `const ENTER_FRAMES = 3` and `const EXIT_FRAMES = 5`
    - Export `function updateGuard(state: GuardState, raw: boolean): void` — implements hysteresis: if raw && !state.active: increment consecutiveFrames, if >= ENTER_FRAMES set active=true and reset counter; if !raw && state.active: increment consecutiveFrames, if >= EXIT_FRAMES set active=false and reset counter; else reset consecutiveFrames to 0.
  </action>
  <verify>
    <automated>cd fps && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - fps/src/lib/springPhysics.ts exports `SpringState` and `stepSpring`
    - `stepSpring` performs semi-implicit Euler: `state.vel += force * dt` BEFORE `state.pos += state.vel * dt` (velocity updated first)
    - fps/src/lib/guardDetection.ts exports `GuardState`, `isGuardPose`, `updateGuard`
    - `updateGuard` uses ENTER_FRAMES=3 and EXIT_FRAMES=5 constants
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>springPhysics and guardDetection modules created and compile cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Wire spring extension + opponent lerp + guard detection into useGameRenderer</name>
  <files>fps/src/hooks/useGameRenderer.ts</files>
  <read_first>
    - fps/src/hooks/useGameRenderer.ts — read the full file as created in Plan 14-01 before editing; understand all existing refs and tick structure
    - fps/src/lib/springPhysics.ts — SpringState interface and stepSpring signature (just created)
    - fps/src/lib/guardDetection.ts — GuardState interface, isGuardPose, updateGuard (just created)
    - fps/src/lib/velocity.ts — computeWristPeakSpeed signature and TimedFrame interface
    - .planning/phases/14-three-js-renderer-game-loop/14-RESEARCH.md Q3 (spring usage in tick), Q9 (lerp smoothing for opponent), D-04 (velocity-scaled extension)
  </read_first>
  <action>
    Modify fps/src/hooks/useGameRenderer.ts:

    Add imports at top: `import { stepSpring, type SpringState } from '../lib/springPhysics'`, `import { isGuardPose, updateGuard, type GuardState } from '../lib/guardDetection'`, `import { computeWristPeakSpeed, type TimedFrame } from '../lib/velocity'`

    Add new useRef entries inside useGameRenderer (before init useEffect):
    - `springStateRef = useRef<{ left: SpringState; right: SpringState }>({ left: { pos: 0, vel: 0 }, right: { pos: 0, vel: 0 } })`
    - `guardStateRef = useRef<GuardState>({ active: false, consecutiveFrames: 0 })`
    - `frameBufferRef = useRef<TimedFrame[]>([])` — rolling 5-frame buffer for computeWristPeakSpeed
    - `opponentTargetRef = useRef<{ lShoulder: THREE.Vector3, lElbow: THREE.Vector3, lWrist: THREE.Vector3, rShoulder: THREE.Vector3, rElbow: THREE.Vector3, rWrist: THREE.Vector3 } | null>(null)` — lerp targets from latest MsgFpsState

    In tick function, replace the current player arm update block with:
    1. Push current keypoints into frameBufferRef (with `t: performance.now()`), keep last 5 frames only.
    2. Call `computeWristPeakSpeed(frameBufferRef.current, 'left')` and `'right'` → scale to spring target [0, 1]: `target = Math.min(1.0, peakSpeed / 4.0)` (speed of 4 m/s maps to full extension; tune 4.0 if feel is wrong).
    3. Call `stepSpring(springStateRef.current.left, leftTarget, dt)` and right.
    4. Apply spring extension: for each forearm mesh, set `mesh.scale.z = 1.0 + springStateRef.current[side].pos * 0.4` (spring pos drives Z stretch — 0.4 scale factor means full extension = 40% forearm stretch). Keep `updateArmSegment` call for positioning but apply scale.z after.

    Replace opponent arm direct-assignment with lerp:
    1. When `latestSocketRef.current.lastFpsState` is non-null, update `opponentTargetRef.current` with fresh keypointToWorld values from the 6 MsgFpsState landmarks.
    2. Each frame, if opponentTargetRef.current is non-null, lerp each opponent arm Vector3 toward its target: `const alpha = 1 - Math.exp(-12 * dt); opponentCurrentLShoulder.lerp(opponentTargetRef.current.lShoulder, alpha)`. Store current opponent positions in a separate `opponentCurrentRef` (initialized to origin Vector3s). Call `updateArmSegment` with the lerped current positions.

    Add guard detection in tick function (after reading keypoints):
    - `const rawGuard = isGuardPose(latestKeypointsRef.current)`
    - `updateGuard(guardStateRef.current, rawGuard)`
    - The `guardStateRef` is already a useRef — Plan 14-04 will read `guardStateRef.current.active` via a ref exposed in the return value.

    Expose guardStateRef: change useGameRenderer return type from `void` to `{ guardStateRef: React.MutableRefObject<GuardState> }` and return `{ guardStateRef }`. Update GameRenderer.tsx to accept the return value (destructure but ignore — Plan 14-04 will use it).
  </action>
  <verify>
    <automated>cd fps && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - fps/src/hooks/useGameRenderer.ts imports `stepSpring`, `computeWristPeakSpeed`, `isGuardPose`, `updateGuard`
    - `springStateRef` is initialized with `{ left: { pos: 0, vel: 0 }, right: { pos: 0, vel: 0 } }`
    - Tick function calls `stepSpring` for both left and right arms each frame — `grep -c "stepSpring" fps/src/hooks/useGameRenderer.ts` returns >= 2
    - Tick function calls `1 - Math.exp(` for frame-rate-independent lerp — `grep -c "Math.exp" fps/src/hooks/useGameRenderer.ts` returns >= 1
    - Tick function calls `updateGuard` each frame — `grep -c "updateGuard" fps/src/hooks/useGameRenderer.ts` returns >= 1
    - useGameRenderer returns `{ guardStateRef }` (no longer void)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Spring physics active on player arms, opponent arms lerp smoothly, guard detection running each frame</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MsgFpsState → opponentTargetRef | Server-supplied keypoints set lerp targets; attacker controls opponent arm positions |
| Player keypoints → spring target | Derived from MediaPipe landmarks; NaN could corrupt spring state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Tampering | opponentTargetRef updated from server MsgFpsState | accept | Opponent arm positions only affect visual rendering, not game logic. No server-trust boundary violation — server is authoritative. |
| T-14-02-02 | Denial of Service | Spring state diverges (NaN propagation) | mitigate | In stepSpring, add guard: `if (!isFinite(target)) return` before computing force. If state.pos becomes NaN, reset: `if (!isFinite(state.pos)) { state.pos = 0; state.vel = 0; }` |
| T-14-02-03 | Tampering | guardStateRef.active read by Plan 14-04 for damage reduction | accept | Guard detection is client-side and only reduces incoming damage display. The server performs authoritative guard checks in fps-boxing-plugin; client guard state is cosmetic/UX only. |
| T-14-02-04 | Information Disclosure | frameBufferRef retains keypoint history | accept | 5-frame buffer contains only the player's own pose data; no PII concern; cleared on component unmount via useEffect cleanup. |
</threat_model>

<verification>
After plan completes:
1. `npm run build` exits 0
2. In the running game: throw a fast punch at the webcam — forearm cylinder should visibly stretch and bounce back
3. Slowly move an arm — no stretch (below velocity threshold)
4. Raise both wrists above shoulders — guardStateRef.current.active becomes true after 3 frames (add temporary `console.log` during dev to verify, remove before commit)
5. Opponent arm positions (when server sends MsgFpsState) should animate smoothly, not snap
</verification>

<success_criteria>
- springPhysics.ts exports SpringState + stepSpring with semi-implicit Euler
- guardDetection.ts exports isGuardPose + GuardState + updateGuard with hysteresis
- useGameRenderer tick integrates spring extension on player forearms scaled by wrist velocity
- Opponent arm positions lerp frame-rate-independently (lambda=12, exp-based)
- Guard detection runs every frame and is exposed via guardStateRef return value
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/14-three-js-renderer-game-loop/14-02-SUMMARY.md`
</output>
