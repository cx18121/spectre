---
phase: 08-dance-ux-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DESIGN.md
  - PRODUCT.md
autonomous: true
requirements:
  - DDES-01
  - DDES-02
  - DDES-03

must_haves:
  truths:
    - "DESIGN.md has a Dance Game section that a Phase 9 implementor can read and implement without any further design decisions"
    - "Score display is fully specified: layout, font token, size, decimal precision, player side assignment"
    - "Beat indicator is fully specified: draining bar mechanics, color tokens, label format, reset trigger"
    - "Target pose skeleton is fully specified: canvas position, scale, opacity value, color tokens, transition timing"
    - "Round end copy is specified with exact format strings and explicit prohibition of boxing vocabulary"
    - "Match end screen is specified: score layout, winner highlight token, label copy, no HP or KO elements"
    - "PRODUCT.md has a Game Modes section distinguishing boxing (combat register) from dance (performance register)"
  artifacts:
    - path: "DESIGN.md"
      provides: "Dance Game section with all sub-sections"
      contains: "## Dance Game"
    - path: "DESIGN.md"
      provides: "Dance HUD spec"
      contains: "### Dance HUD"
    - path: "DESIGN.md"
      provides: "Beat indicator spec"
      contains: "### Beat Indicator"
    - path: "DESIGN.md"
      provides: "Target pose skeleton spec"
      contains: "### Target Pose Skeleton"
    - path: "DESIGN.md"
      provides: "Dance round end spec"
      contains: "### Dance Round End"
    - path: "DESIGN.md"
      provides: "Dance match end spec"
      contains: "### Dance Match End"
    - path: "PRODUCT.md"
      provides: "Two-mode tone definition"
      contains: "## Game Modes"
  key_links:
    - from: "DESIGN.md Dance HUD"
      to: "overlay/src/components/HudLayer.tsx"
      via: "DanceHud adapts the two-row .hud-band shell"
      pattern: "hud-band"
    - from: "DESIGN.md Target Pose Skeleton"
      to: "engine/engine-core/bindings/MsgDanceBeat.ts"
      via: "target_pose Array of x y z visibility per keypoint"
      pattern: "target_pose"
    - from: "DESIGN.md Beat Indicator"
      to: "MsgDanceBeat.beat and MsgDanceBeat.total_beats"
      via: "N / total_beats label above the draining bar"
      pattern: "total_beats"
---

<objective>
Write the complete design specification for the dance game experience. This is a documentation-only phase — no code is written. The output is DESIGN.md (dance section appended) and PRODUCT.md (two-mode tone update). Phase 9 implements directly against this spec without revisiting design decisions.

Purpose: Every visual element for the dance game needs an unambiguous spec before a pixel of implementation begins. Phase 9 will create DanceHud, target pose skeleton rendering in Pixi.js, and the dance match end screen — all grounded in this document.

Output:
- DESIGN.md gains a Dance Game section with five sub-sections: Dance HUD, Beat Indicator, Target Pose Skeleton, Dance Round End, Dance Match End
- PRODUCT.md gains a Game Modes section defining boxing (combat register) vs dance (performance register)
</objective>

<execution_context>
@/Users/charliexue/School/Comps/spectre/.planning/phases/08-dance-ux-design/08-01-PLAN.md
</execution_context>

<context>
@/Users/charliexue/School/Comps/spectre/.planning/ROADMAP.md
@/Users/charliexue/School/Comps/spectre/.planning/REQUIREMENTS.md
@/Users/charliexue/School/Comps/spectre/.planning/phases/08-dance-ux-design/08-CONTEXT.md
@/Users/charliexue/School/Comps/spectre/DESIGN.md
@/Users/charliexue/School/Comps/spectre/PRODUCT.md

<interfaces>
Wire protocol the design spec must be grounded in.

From engine/engine-core/bindings/MsgDanceBeat.ts:
  MsgDanceBeat.beat: number (current beat index)
  MsgDanceBeat.total_beats: number (total beats in round)
  MsgDanceBeat.target_pose: Array<[number, number, number, number]>
    -- each element: [x, y, z, visibility] per keypoint (hip-centred Y-up coords)

MsgDanceScore wire payload (from shared/protocol.ts):
  MsgDanceScore.beat: number
  MsgDanceScore.scores: [number, number]  -- [p1_cumulative, p2_cumulative], one decimal precision

From overlay/src/components/HudLayer.tsx -- structure being adapted:
  HudLayer two-row structure:
    Row 1: .hud-names  -- P1 name/wins | center timer+round+room | P2 name/wins
    Row 2: .hud-bars   -- P1 HP track | separator | P2 HP track

  DanceHud adapts same .hud-band shell:
    Row 1 (names/beat): P1 label | beat indicator (centre column) | P2 label  [no win dots]
    Row 2 (scores):     P1 score | vs | P2 score  [replaces HP bars]

From DESIGN.md Motion section:
  Animation timing already established:
    UI overlays: 150ms ease-out-quart appear, 120ms ease-in disappear
    Beat-swap skeleton fade uses these exact values. No new timing tokens needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Dance HUD and Beat Indicator sections to DESIGN.md</name>
  <files>DESIGN.md</files>
  <read_first>
    - DESIGN.md (full file -- read before appending to understand existing token names, section structure, and typography scale; confirm exact token spelling for --text-secondary, --bg-surface, --type-label, --type-hud-timer, --gold)
    - .planning/phases/08-dance-ux-design/08-CONTEXT.md (decisions D-01 through D-05)
    - overlay/src/components/HudLayer.tsx (two-row .hud-band structure being adapted)
    - engine/engine-core/bindings/MsgDanceBeat.ts (wire payload confirming beat / total_beats field names)
  </read_first>
  <action>
Append a "## Dance Game" section to DESIGN.md after the existing "## Lobby" section. The section must contain sub-sections "### Dance HUD" and "### Beat Indicator" with the following exact content.

The "## Dance Game" section preamble:

The dance game shares the ink-black aesthetic and OKLCH token system of boxing. The register shifts from "combat" to "performance" — scores accumulate rather than deplete, copy uses scoreboard vocabulary instead of fight vocabulary. No HP bars. No win dots. No KO. The layout adapts the same .hud-band shell.

"### Dance HUD" sub-section:

The .hud-band contains two rows, identical outer structure to the boxing HUD but different contents.

Row 1 — Names and Beat Indicator:
Layout: three columns in a flex row. P1 label left, beat indicator centre, P2 label right.
- P1 column: "P1" label — Inter 900 12px letter-spacing 0.1em uppercase --text-primary. No win dots.
- P2 column: "P2" label — same spec, right-aligned. No win dots.
- Centre column: beat indicator (see ### Beat Indicator below). Width: 40% of the .hud-band total width.

Row 2 — Scores:
Replaces the HP bars row. Contains P1 score, "vs" separator, P2 score in a single flex row.
- P1 score: Inter 900, 36px, --text-primary, left-aligned within P1 column. Format: one decimal precision — e.g. "11.7". Zero-state: "0.0".
- "vs" separator: Inter 700, 12px, letter-spacing 0.1em, uppercase, --text-secondary, centered.
- P2 score: Inter 900, 36px, --text-primary, right-aligned within P2 column. Same format.
- No bar, no fill, no color coding by score value.

Score sizing note: 36px matches the existing --type-hud-timer token (36px 900) for visual parity with the boxing timer. No new typography token is needed.

HUD band structural rules (unchanged from boxing):
- Background: --bg-mid. Border: Level 1 elevation (1px --gold 20% opacity, inset highlight). Same CSS classes reused: .hud-band, .hud-layer.
- No win dots rendered anywhere in the dance HUD.
- No HP bar tracks or fills in the dance HUD.

"### Beat Indicator" sub-section:

The beat indicator occupies the centre column of Row 1 of the Dance HUD. It communicates: current beat count (N / total_beats) and time remaining until the next beat boundary (draining bar).

Structure (top to bottom within the centre column):
1. Beat count label: "N / total_beats" — Inter 700, 12px, letter-spacing 0.1em, uppercase, --text-secondary. N and total_beats are live values from MsgDanceBeat fields beat and total_beats. Example: "4 / 16".
2. Draining bar: full-width within the centre column, height 4px.
   - Track: background --bg-surface, no border.
   - Fill: --text-secondary.
   - Behavior: resets to 100% width on each dance_beat event, drains linearly to 0% by the next expected beat boundary (i.e., over one beat duration).
   - Animation: CSS transition: width 0ms on reset (hard snap to full), then transition: width {beat_duration_ms}ms linear for drain.
   - No border-radius. No glow.

Color rationale: --text-secondary for fill is intentionally neutral — it must not compete with the target pose skeleton silhouette rendered in the Pixi canvas below.
  </action>
  <verify>
    <automated>grep -c "### Dance HUD" /Users/charliexue/School/Comps/spectre/DESIGN.md && grep -c "### Beat Indicator" /Users/charliexue/School/Comps/spectre/DESIGN.md && grep -c "N / total_beats" /Users/charliexue/School/Comps/spectre/DESIGN.md && grep -c "dance_beat" /Users/charliexue/School/Comps/spectre/DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "## Dance Game" DESIGN.md returns 1
    - grep -c "### Dance HUD" DESIGN.md returns 1
    - grep -c "### Beat Indicator" DESIGN.md returns 1
    - grep -c "N / total_beats" DESIGN.md returns 1 (beat count label format string present)
    - grep -c "dance_beat" DESIGN.md returns at least 1 (bar reset trigger event named)
    - grep -c "36px" DESIGN.md returns at least 1 (score number size specified)
    - grep -c "40%" DESIGN.md returns at least 1 (centre column width specified)
    - grep -c "linear" DESIGN.md returns at least 1 (drain animation keyword present)
    - The new section is appended after the existing "## Lobby" section, not inserted within it
  </acceptance_criteria>
  <done>DESIGN.md contains "## Dance Game" with "### Dance HUD" and "### Beat Indicator" sub-sections. Every D-01 through D-05 decision is captured as written spec with no ambiguous terms requiring further clarification for Phase 9.</done>
</task>

<task type="auto">
  <name>Task 2: Append Target Pose Skeleton, Dance Round End, and Dance Match End to DESIGN.md</name>
  <files>DESIGN.md</files>
  <read_first>
    - DESIGN.md (read after Task 1 completes -- confirm Dance Game section was appended and locate its end)
    - .planning/phases/08-dance-ux-design/08-CONTEXT.md (decisions D-06 through D-12)
    - engine/engine-core/bindings/MsgDanceBeat.ts (target_pose: Array<[number, number, number, number]> -- x, y, z, visibility)
    - overlay/src/components/RoundOverlay.tsx (existing boxing round-end and match-end patterns to adapt)
  </read_first>
  <action>
Append three sub-sections to the "## Dance Game" section in DESIGN.md, continuing after "### Beat Indicator". Write the following content.

"### Target Pose Skeleton" sub-section:

The target pose skeleton is a ghost silhouette rendered in the Pixi.js canvas layer (PixiCanvas). It shows the ideal body position the player should match for the current beat.

Position and scale:
- Rendered at the horizontal centre of the canvas.
- Vertical position aligned with the live player silhouettes (same Y-origin, human scale).
- Scale matches the player silhouettes -- the ghost body should appear life-size alongside the live players.
- Centred in the gap between the two player silhouettes; does not overlap either player's canvas zone.

Visual style (DDES-03):
- Keypoints: filled circles, color --text-dim, at 40% opacity.
- Bone lines: stroked lines connecting standard pose keypoints, color --text-dim, at 40% opacity.
- No fill color gradient, no accent color. Ghost-only appearance.
- Do not use --text-secondary for the skeleton -- --text-dim is dimmer and avoids competing with the beat indicator bar.

Wire data:
The skeleton is drawn from MsgDanceBeat.target_pose: an array of [x, y, z, visibility] tuples (one per keypoint, hip-centred Y-up normalised coordinates). Only keypoints with visibility >= 0.5 should be drawn. Bone lines connect standard MediaPipe Pose landmark indices.

Transition on beat swap:
On each dance_beat event the old target pose fades out and the new pose fades in:
1. Fade-out: opacity 40% to 0%, duration 150ms, easing ease-out-quart. (Same as UI overlays disappear in the Motion section -- applied to the Pixi Graphics object alpha, not a CSS element.)
2. After fade-out completes: update the skeleton geometry to the new target_pose keypoints.
3. Fade-in: opacity 0% to 40%, duration 150ms, easing ease-in.
4. Total swap time: 300ms.

Rendering note for Phase 9: The skeleton is a static Pixi Graphics object redrawn from keypoints. Animate opacity via gsap (already used in the overlay) or a manual requestAnimationFrame ticker. Animate the whole Graphics object alpha -- do not animate individual keypoints.

"### Dance Round End" sub-section:

The dance round end screen reuses the .round-flash overlay component from RoundOverlay.tsx with different copy. No new component is required.

Copy format:
- Winner determined: "ROUND N -- P1 LEADS" or "ROUND N -- P2 LEADS" (N is the round number, winning player label used).
- Tie: "ROUND N -- TIED"
- No "WINS", no "KO", no "TIME" -- boxing vocabulary is prohibited in dance round end copy.

Below the headline: Render both cumulative scores in --text-secondary, Inter 700 18px. Format: "P1: 11.7  P2: 9.2". Same horizontal layout as the HUD score row -- P1 left, P2 right.

Timing and style: Identical to boxing round end -- .round-flash Achafont display, existing 2200ms visibility window, existing motion spec. Only the text content changes.

Prohibited elements: No HP bar. No win dots. No "KO" text. No "TIME" text. No reference to damage.

"### Dance Match End" sub-section:

The dance match end screen replaces the boxing .match-end-overlay for dance rooms. It is a new layout within the same overlay mechanism.

Layout (top to bottom):
1. "WINNER" label -- Inter 900, 12px, letter-spacing 0.14em, uppercase, color matches winner's accent (--accent for P1, --accent-p2 for P2).
2. Score row -- two large score numbers side by side with "vs" separator:
   - Winner's score: Inter 900, clamp(48px, 8vw, 96px), accent color (--accent or --accent-p2 based on winner slot). Large size is the primary visual signal.
   - "vs" separator: Inter 700, 16px, --text-secondary, centred.
   - Loser's score: Inter 900, clamp(48px, 8vw, 96px), --text-secondary (de-emphasised, not hidden).
3. Player labels below each score: "P1" / "P2" -- Inter 900 12px letter-spacing 0.1em --text-secondary.
4. Rematch button (unchanged from boxing -- same .rematch-btn style).

Winner determination: Highest cumulative score at match end wins. Tie: display "TIED" instead of "WINNER", no accent highlight on either score.

Prohibited elements: No "K.O." text. No HP bars. No damage or hits stats. No reference to rounds won count. Only data shown is final scores and the winner declaration.

Elevation and background: Same match-end-overlay background treatment as boxing (Level 3 elevation, --bg-deep at high opacity). The spec does not change the overlay shell -- only its content.
  </action>
  <verify>
    <automated>grep -c "### Target Pose Skeleton" /Users/charliexue/School/Comps/spectre/DESIGN.md && grep -c "### Dance Round End" /Users/charliexue/School/Comps/spectre/DESIGN.md && grep -c "### Dance Match End" /Users/charliexue/School/Comps/spectre/DESIGN.md && grep -c "40% opacity" /Users/charliexue/School/Comps/spectre/DESIGN.md && grep -c "ROUND N" /Users/charliexue/School/Comps/spectre/DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "### Target Pose Skeleton" DESIGN.md returns 1
    - grep -c "### Dance Round End" DESIGN.md returns 1
    - grep -c "### Dance Match End" DESIGN.md returns 1
    - grep -c "40% opacity" DESIGN.md returns at least 1 (skeleton opacity value present)
    - grep -c "ROUND N" DESIGN.md returns at least 1 (round end copy template present)
    - grep -c "WINNER" DESIGN.md returns at least 1 (match end winner label present)
    - grep -c "visibility >= 0.5" DESIGN.md returns 1 (keypoint visibility threshold specified)
    - grep -c "clamp(48px" DESIGN.md returns at least 1 (match end score size present)
    - grep -c "K.O." DESIGN.md returns 1 (prohibited element explicitly called out)
    - grep -c "--text-dim" DESIGN.md returns at least 1 (skeleton color token named)
    - The phrase "No HP bar" appears in the Dance Round End sub-section
    - The phrase "No win dots" appears in the Dance Round End sub-section
  </acceptance_criteria>
  <done>DESIGN.md Dance Game section is complete with all five sub-sections. Every D-06 through D-12 decision is captured. Phase 9 can implement DanceHud, target pose skeleton, round end, and match end without returning to design.</done>
</task>

<task type="auto">
  <name>Task 3: Update PRODUCT.md with two-mode game tone definition</name>
  <files>PRODUCT.md</files>
  <read_first>
    - PRODUCT.md (full file -- read before editing to understand existing sections: Product Purpose, Users, Register, Tone, Anti-References, Strategic Principles)
    - .planning/phases/08-dance-ux-design/08-CONTEXT.md (DDES-02 requirement: dance tone = "performance" register, same dark aesthetic, not "combat")
    - .planning/REQUIREMENTS.md (DDES-02 exact requirement text)
  </read_first>
  <action>
Append a "## Game Modes" section to PRODUCT.md after the existing "## Strategic Principles" section. Write the following content.

"## Game Modes" section:

Spectre runs two game modes that share the same aesthetic but operate in different emotional registers.

### Boxing

Register: combat. Players throw punches and kicks; HP depletes; rounds end in KO or decision. Copy uses fight vocabulary: KO, ROUND N -- P1 WINS, damage, hits. The dramatic peaks (KO text, countdown, hit flash) are designed to match the physicality of punching.

Tone guidance: intense, immediate, zero-latency feedback. Every hit should feel consequential. The HP bar is a clock and a threat.

### Dance

Register: performance. Players match pose targets scored beat by beat; scores accumulate from zero; rounds end when beats are exhausted. Copy uses scoreboard vocabulary: ROUND N -- P1 LEADS, TIED, final scores. No KO, no damage, no "WINS" in the fight sense.

Tone guidance: disciplined rhythm over chaotic aggression. The ghost skeleton is an invitation, not a challenge. The score gap between players should be readable at a glance -- numbers dominate, not colors or animations.

Shared aesthetic rules (both modes):
- Same OKLCH color system, same ink-black backgrounds, same Inter + Achafont type pairing.
- Achafont for dramatic display moments in both modes (round flash, match end headline).
- No neon, no RGB gradients, no glassmorphism -- the anti-references in this document apply equally.
- The overlay is the product in both modes. Physical action in the room drives what spectators see on screen.

Anti-references for dance specifically:
- Dance game show aesthetics (sparkles, bright pastels, star ratings)
- Music rhythm game chrome (note highways, multiplier counters, score combos with pop-up labels)
- Generic fitness app UI (progress rings, streak badges, calorie counters)
  </action>
  <verify>
    <automated>grep -c "## Game Modes" /Users/charliexue/School/Comps/spectre/PRODUCT.md && grep -c "### Boxing" /Users/charliexue/School/Comps/spectre/PRODUCT.md && grep -c "### Dance" /Users/charliexue/School/Comps/spectre/PRODUCT.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "## Game Modes" PRODUCT.md returns 1
    - grep -c "### Boxing" PRODUCT.md returns 1
    - grep -c "### Dance" PRODUCT.md returns 1
    - grep -c "performance" PRODUCT.md returns at least 1 (dance register named)
    - grep -c "combat" PRODUCT.md returns at least 1 (boxing register named)
    - grep -c "scoreboard" PRODUCT.md returns at least 1 (dance vocabulary described)
    - grep -c "Anti-references for dance" PRODUCT.md returns 1 (dance-specific anti-references present)
    - grep -c "sparkles" PRODUCT.md returns 1 (dance show aesthetic explicitly called out)
    - The phrase "same OKLCH color system" or equivalent shared-aesthetic language is present
    - PRODUCT.md existing sections (Product Purpose, Users, Tone, Anti-References, Strategic Principles) are intact and unmodified
  </acceptance_criteria>
  <done>PRODUCT.md has a Game Modes section that defines boxing as "combat" register and dance as "performance" register, names shared aesthetic rules, and lists dance-specific anti-references. DDES-02 is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This phase produces only static Markdown documents. No user input, no network calls, no data storage, no executable code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | — | DESIGN.md, PRODUCT.md | accept | Documentation-only phase. No attack surface exists. No user input is processed, no network endpoints are created, no data is stored. All STRIDE categories are out of scope for this phase. |
</threat_model>

<verification>
After all three tasks complete, run the following to confirm phase output is complete:

1. DESIGN.md has all five dance sub-sections:
   grep -c "## Dance Game" DESIGN.md          # must be 1
   grep -c "### Dance HUD" DESIGN.md           # must be 1
   grep -c "### Beat Indicator" DESIGN.md      # must be 1
   grep -c "### Target Pose Skeleton" DESIGN.md # must be 1
   grep -c "### Dance Round End" DESIGN.md     # must be 1
   grep -c "### Dance Match End" DESIGN.md     # must be 1

2. All locked design decisions represented in DESIGN.md:
   grep -c "N / total_beats" DESIGN.md          # D-04 beat count label
   grep -c "40% opacity" DESIGN.md              # D-07 skeleton opacity
   grep -c "150ms" DESIGN.md                    # D-08 transition timing
   grep -c "ROUND N" DESIGN.md                  # D-09 round end copy
   grep -c "WINNER" DESIGN.md                   # D-11 match end label
   grep -c "clamp(48px" DESIGN.md               # D-11 score size

3. PRODUCT.md has two-mode section:
   grep -c "## Game Modes" PRODUCT.md           # must be 1
   grep -c "performance" PRODUCT.md             # DDES-02 dance register
</verification>

<success_criteria>
1. DESIGN.md has a complete "## Dance Game" section with all five sub-sections (Dance HUD, Beat Indicator, Target Pose Skeleton, Dance Round End, Dance Match End) — sufficient for Phase 9 to implement without any design ambiguity.
2. PRODUCT.md acknowledges both game modes; the "## Game Modes" section defines boxing as combat register and dance as performance register with distinct tone guidance and anti-references.
3. All twelve locked decisions from 08-CONTEXT.md (D-01 through D-12) are reflected in the written spec with exact token names, sizes, and copy strings.
4. No deferred ideas from 08-CONTEXT.md are introduced.
5. Every grep verification in the verification section above returns the expected count.
</success_criteria>

<output>
After completion, create .planning/phases/08-dance-ux-design/08-01-SUMMARY.md with:
- Files modified: DESIGN.md (Dance Game section added), PRODUCT.md (Game Modes section added)
- Sections added to DESIGN.md: Dance HUD, Beat Indicator, Target Pose Skeleton, Dance Round End, Dance Match End
- Key design tokens specified: --text-dim (skeleton), --text-secondary (beat bar fill), --bg-surface (beat bar track), --accent/--accent-p2 (match end winner highlight)
- Key copy strings: "N / total_beats", "ROUND N -- P1 LEADS", "ROUND N -- TIED", "WINNER"
- Phase 9 can now implement DanceHud, target pose skeleton in Pixi.js, dance round end, and dance match end without returning to design
</output>
