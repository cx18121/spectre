---
phase: 13.1-punch-classifier-model
verified: 2026-05-14T02:20:00Z
status: deferred
score: 10/12 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Verify >85% accuracy across 5 punch classes"
    expected: "Trained ONNX model achieves >85% val_acc on held-out test set"
    why_human: "No training data is committed; placeholder model has all-zero weights. This criterion requires offline training on BoxingVI + webcam recordings and empirical evaluation."
  - test: "Verify ONNX model is <500KB and runs <2ms per inference in onnxruntime-web WASM"
    expected: "Real trained INT8 model file size <500KB; browser console shows inference < 2ms"
    why_human: "Placeholder model is 9.6KB (trivially small). Size and latency must be measured from a real trained model in a browser environment."
---

# Phase 13.1: Punch Classifier Model — Verification Report

**Phase Goal:** A trained ONNX punch classifier (<500KB, <2ms inference) distinguishes jab/cross/hook_l/hook_r/guard from MediaPipe landmarks with >85% accuracy across diverse users, integrated into fps/ as usePunchClassifier hook
**Verified:** 2026-05-14T02:20:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                                     | Status     | Evidence                                                                                                             |
|----|-----------------------------------------------------------------------------------------------------------|------------|----------------------------------------------------------------------------------------------------------------------|
| 1  | ml/ directory exists with requirements.txt, README.md, and two runnable scripts                           | ✓ VERIFIED | `ls ml/` shows README.md, requirements.txt, data/, models/, scripts/. Both scripts exit 0 on --help.                |
| 2  | extract_keypoints.py accepts --input-dir and --output-dir arguments and prints usage on --help            | ✓ VERIFIED | `python ml/scripts/extract_keypoints.py --help` exits 0, shows all 5 arguments including --dry-run.                 |
| 3  | record_webcam.py accepts --output-dir and --label arguments and prints usage on --help                    | ✓ VERIFIED | `python ml/scripts/record_webcam.py --help` exits 0, shows all 5 arguments including --dry-run.                     |
| 4  | ml/data/ and ml/models/ are present as empty dirs (gitkeep); .mp4/.pt artifacts are gitignored            | ✓ VERIFIED | `.gitkeep` files exist in both dirs. `.gitignore` contains `ml/data/**/*.mp4`, `ml/models/*.pt`, `ml/models/*.onnx`. |
| 5  | train.py --dry-run exits 0 and prints model architecture + param count                                    | ✓ VERIFIED | Exits 0. Prints PunchMLP architecture, 164,613 params (plan said ~155K; actual is ~165K — acceptable variance).     |
| 6  | fps/public/models/punch_classifier_int8.onnx is a valid ONNX file with input='input', output='logits'    | ✓ VERIFIED | `onnx.checker.check_model()` passes. Graph input name: 'input', output name: 'logits'. File size: 9.6KB.            |
| 7  | normalizeWindow() implements shoulder-midpoint + shoulder-width normalization matching Python train.py    | ✓ VERIFIED | Both normalizeWindow tests pass. Formula in .ts and normalize_window() in train.py use identical midpoint + sw logic. |
| 8  | usePunchClassifier creates one InferenceSession at mount; returns { type, confidence, speed }             | ✓ VERIFIED | Test 4 (mockCreate called exactly once) passes. Tests 1–6 all pass (8 total tests).                                  |
| 9  | ort.env.wasm.wasmPaths is set at module init level, not inside hook body                                  | ✓ VERIFIED | Line 11 in usePunchClassifier.ts: `ort.env.wasm.wasmPaths = '...'` — before function definition at line 54.         |
| 10 | PunchType and MsgPunchDetected defined locally in usePunchClassifier.ts; shared/protocol.ts untouched     | ✓ VERIFIED | `grep 'MsgPunchDetected\|PunchType' shared/protocol.ts` returns empty. Both types exported from usePunchClassifier.ts. |
| 11 | JOINT_INDICES [11,12,13,14,15,16,23,24] consistent across extract_keypoints.py, train.py, usePunchClassifier.ts | ✓ VERIFIED | All three files use exactly [11, 12, 13, 14, 15, 16, 23, 24] (TypeScript uses LANDMARK constants resolving to same values). |
| 12 | 8 Vitest tests pass (2 normalizeWindow, 6 usePunchClassifier); full 79-test suite has no regressions       | ✓ VERIFIED | `npx vitest run src/lib/normalizeWindow.test.ts src/hooks/usePunchClassifier.test.ts` → 8 passed. Full suite: 79 passed. |

**Score:** 10/12 truths verified (2 require human — post-training empirical evaluation)

Note: Truths 11 and 12 in the roadmap success criteria ("Temporal MLP achieves >85% accuracy" and "model is <500KB, <2ms inference") cannot be verified without real trained weights. These are deferred to post-training evaluation and require human verification.

### Required Artifacts

| Artifact                                              | Expected                                      | Status     | Details                                                  |
|-------------------------------------------------------|-----------------------------------------------|------------|----------------------------------------------------------|
| `ml/README.md`                                        | Pipeline documentation                        | ✓ VERIFIED | Present, covers setup → record → extract → train → export → deploy |
| `ml/requirements.txt`                                 | 7 Python dependencies                         | ✓ VERIFIED | torch, onnxruntime, onnx, numpy, mediapipe, opencv-python, scikit-learn |
| `ml/scripts/extract_keypoints.py`                     | MediaPipe extraction CLI                      | ✓ VERIFIED | Runnable, --help exits 0, --dry-run exits 0              |
| `ml/scripts/record_webcam.py`                         | Webcam recording CLI                          | ✓ VERIFIED | Runnable, --help exits 0, --dry-run flag present         |
| `ml/scripts/train.py`                                 | Full training loop with PunchMLP              | ✓ VERIFIED | --dry-run: 164,613 params, logits (4,5), exits 0         |
| `ml/scripts/export_onnx.py`                           | ONNX export (opset 17)                        | ✓ VERIFIED | Present and runnable (--help exits 0)                    |
| `ml/scripts/quantize.py`                              | INT8 dynamic quantization                     | ✓ VERIFIED | Present and runnable (--help exits 0)                    |
| `fps/public/models/punch_classifier_int8.onnx`        | Valid placeholder ONNX                        | ✓ VERIFIED | 9.6KB, passes onnx.checker, correct input/output names   |
| `fps/public/models/README.md`                         | Placeholder state documentation               | ✓ VERIFIED | Present, documents placeholder state and replacement steps |
| `fps/src/lib/normalizeWindow.ts`                      | normalizeWindow() exported function           | ✓ VERIFIED | Exports normalizeWindow, 2 tests pass                    |
| `fps/src/lib/normalizeWindow.test.ts`                 | 2 Vitest tests                                | ✓ VERIFIED | Both pass: midpoint at origin + shoulder width = 1.0     |
| `fps/src/hooks/usePunchClassifier.ts`                 | usePunchClassifier hook + exports             | ✓ VERIFIED | Exports usePunchClassifier, PunchType, MsgPunchDetected, PunchClassifierResult |
| `fps/src/hooks/usePunchClassifier.test.ts`            | 6 Vitest tests                                | ✓ VERIFIED | All 6 pass (null on null, warm-up, shape, once-create, jab, confidence threshold) |
| `fps/package.json` onnxruntime-web@^1.26.0            | Dependency declared                           | ✓ VERIFIED | Present in dependencies; installed in node_modules       |

### Key Link Verification

| From                              | To                                       | Via                                       | Status     | Details                                                        |
|-----------------------------------|------------------------------------------|-------------------------------------------|------------|----------------------------------------------------------------|
| `usePunchClassifier.ts`           | `normalizeWindow.ts`                     | `import { normalizeWindow } from '../lib/normalizeWindow'` | ✓ WIRED | Line 4; called at line 112 on each frame |
| `usePunchClassifier.ts`           | `velocity.ts`                            | `import { computeWristPeakSpeed, LANDMARK }` | ✓ WIRED | Lines 5, 123-124; LANDMARK used to populate JOINT_INDICES |
| `usePunchClassifier.ts`           | `/models/punch_classifier_int8.onnx`     | `ort.InferenceSession.create('/models/punch_classifier_int8.onnx')` | ✓ WIRED | Line 71; inside useEffect with [] deps (mount-only) |
| `train.py normalize_window()`     | `normalizeWindow.ts normalizeWindow()`   | Identical shoulder-midpoint + shoulder-width formula | ✓ WIRED | Both: midpoint = (lSh + rSh)/2, sw = norm(rSh-lSh), clipped at 1e-6 |
| `extract_keypoints.py`            | `ml/data/<class>/<clip>.npy`             | `np.save`                                 | ✓ WIRED | Present in extraction loop (execution path verified by --dry-run) |
| `record_webcam.py`                | `ml/data/<label>/<timestamp>.mp4`        | `cv2.VideoWriter`                         | ✓ WIRED | Present in implementation (verified by --help, --dry-run guard) |

### Data-Flow Trace (Level 4)

| Artifact                    | Data Variable        | Source                          | Produces Real Data                    | Status      |
|-----------------------------|----------------------|---------------------------------|---------------------------------------|-------------|
| `usePunchClassifier.ts`     | `result.type`        | `session.run({ input: tensor })` | Depends on real ONNX weights (placeholder = all-zero) | ✓ FLOWING (placeholder returns uniform logits → type: null; real model replaces) |
| `normalizeWindow.ts`        | `Float32Array`       | `buffer[t][jointIndices[j]]`    | Real keypoints passed from caller     | ✓ FLOWING   |

### Behavioral Spot-Checks

| Behavior                                            | Command                                                                                      | Result                                      | Status  |
|-----------------------------------------------------|----------------------------------------------------------------------------------------------|---------------------------------------------|---------|
| extract_keypoints.py --help exits 0                 | `python ml/scripts/extract_keypoints.py --help`                                              | Shows usage with all 5 args                 | ✓ PASS  |
| record_webcam.py --help exits 0                     | `python ml/scripts/record_webcam.py --help`                                                  | Shows usage with all 5 args                 | ✓ PASS  |
| train.py --dry-run exits 0, prints ~165K params     | `python ml/scripts/train.py --dry-run`                                                       | 164,613 params, logits (4,5), exits 0       | ✓ PASS  |
| ONNX placeholder valid, correct names               | `python -c "import onnx; m=onnx.load('fps/public/models/punch_classifier_int8.onnx'); onnx.checker.check_model(m); print(m.graph.input[0].name, m.graph.output[0].name)"` | `input logits` | ✓ PASS |
| 8 targeted Vitest tests pass                        | `cd fps && npx vitest run src/lib/normalizeWindow.test.ts src/hooks/usePunchClassifier.test.ts` | 8 passed                                  | ✓ PASS  |
| Full 79-test suite passes (no regressions)          | `cd fps && npx vitest run`                                                                   | 79 passed, 13 test files                    | ✓ PASS  |
| wasmPaths at module level                           | `grep 'wasmPaths' fps/src/hooks/usePunchClassifier.ts`                                       | Line 11, before function definition at L54  | ✓ PASS  |
| shared/protocol.ts untouched                        | `grep 'MsgPunchDetected\|PunchType' shared/protocol.ts`                                      | Empty (exit code 1)                         | ✓ PASS  |
| JOINT_INDICES consistent across 3 files             | grep JOINT_INDICES on all 3 files                                                            | All use [11,12,13,14,15,16,23,24]           | ✓ PASS  |

### Requirements Coverage

No formal REQUIREMENTS.md IDs were assigned to Phase 13.1 (ROADMAP.md shows "Requirements: TBD"). The 4 roadmap success criteria are evaluated directly above.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `fps/public/models/punch_classifier_int8.onnx` | N/A | All-zero weights | ℹ️ Info | Intentional placeholder; documented in fps/public/models/README.md; type: null until replaced with real model |

No blocking stubs or placeholder patterns found in TypeScript/Python source files. The ONNX placeholder is intentional per the phase design.

### Human Verification Required

#### 1. Punch Classification Accuracy (>85%)

**Test:** Collect BoxingVI dataset clips, run `python ml/scripts/extract_keypoints.py`, run `python ml/scripts/train.py`, evaluate val_acc on held-out test set.
**Expected:** val_acc > 85% across 5 classes (jab/cross/hook_l/hook_r/guard) after 100 epochs.
**Why human:** No training data is committed to the repo. The placeholder ONNX has all-zero weights. Training requires downloading the BoxingVI dataset and running the pipeline on a developer machine with MediaPipe and PyTorch installed.

#### 2. Model Size < 500KB and Inference < 2ms in Browser

**Test:** After training, quantize the model via `quantize.py`, copy to `fps/public/models/`, load the fps/ dev server, open browser DevTools, observe network size of the .onnx file and timing logs from usePunchClassifier.
**Expected:** .onnx file size < 500KB on disk/network; console shows inference time < 2ms per frame.
**Why human:** Current placeholder is 9.6KB (trivially small, all-zero). Real INT8 model size depends on trained weights. Inference latency requires running in a browser WASM environment — not testable with file checks alone.

### Gaps Summary

No blocking gaps found. All deliverable code artifacts exist, are substantive, and are correctly wired. The two items requiring human verification are success criteria that are architecturally impossible to verify without real training data — this is an expected and intentional state for a "scaffold + hook" phase that precedes data collection and training.

---

_Verified: 2026-05-14T02:20:00Z_
_Verifier: Claude (gsd-verifier)_
