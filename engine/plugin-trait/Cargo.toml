[package]
name = "plugin-trait"
version = "0.1.0"
edition = "2021"

[lib]
name = "plugin_trait"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
